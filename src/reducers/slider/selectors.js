export const getDots = (state) => state.slider.dots;
export const getActiveSlide = (state) => state.slider.activeSlide;
export const getMode = (state) => state.slider.mode;
export const getSlides = (state) => state.slider.slides;
